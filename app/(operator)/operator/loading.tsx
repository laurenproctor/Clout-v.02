import { ListPageSkeleton } from '@/components/loading/list-page-skeleton'

// Group-level navigation fallback for operator pages (list/table shaped).
export default function Loading() {
  return <ListPageSkeleton />
}
